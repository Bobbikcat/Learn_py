# №1
# Почему не нужен? Добавлю я задачу, отработает этот блок,
# а в меню в переменной respond останется None.
# Второй вариант: Я или Кто-то другой хочет инициализировать
# tasks не пустым, и при remove/complete/save произойдет
# то же самое.

# №2 Все еще не понимаю, почему всегда ругается и подчеркивает желтым слово file в теле(40 строка).
# Другими словами, даже если сделать весь блок полностью дефолтным, все равно проблема.
# data = {}
# with open('data.json', 'w') as file:
#    json.dump(data, file, indent=4)  # дефолтный вариант